# Local-only zsh environment overrides.
# Copy to config/zsh/env.local.zsh and set private values there.

# export KNOWLEDGE_REPO_PATH="$HOME/path/to/private/worklog"
# export KNOWLEDGE_REPO_URL="https://github.com/example/private-worklog"

# Optional legacy phpenv setup. Prefer Nix-managed php unless a project needs phpenv.
# export PATH="$HOME/.phpenv/bin:$PATH"
# command -v phpenv >/dev/null 2>&1 && eval "$(phpenv init -)"
